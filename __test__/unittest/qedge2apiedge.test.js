const fs = require('fs');
const path = require('path');

// TODO write tests once qXEdge/qEdge refactor is done


describe('test QEdge2APIEdgeHandler', () => {
  test('setQEdges', () => {
    const QEdge2APIEdgeHandler = require('../../src/qedge2apiedge');
    const handler = new QEdge2APIEdgeHandler();
  });
});
